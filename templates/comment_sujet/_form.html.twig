<div class="uk-flex uk-flex-center uk-width-1-3">
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="uk-button uk-button-text">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
</div>
