<form method="post" action="{{ path('chanel_delete', {'id': chanel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chanel.id) }}">
    <button class="btn">Delete</button>
</form>
